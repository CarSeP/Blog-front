import NotFoundCard from "@/components/NotFoundCard/NotFoundCard";

export default function NotFound() {
  return (
    <NotFoundCard
      title=""
      subTitle="Post no encontrado"
      description="Lo sentimos, el post que estás buscando no existe o ha sido eliminado."
    />
  );
}
