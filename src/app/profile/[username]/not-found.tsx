import NotFoundCard from "@/components/NotFoundCard/NotFoundCard";

export default function NotFound() {
  return (
    <NotFoundCard
      title=""
      subTitle="Usuario no encontrado"
      description="Lo sentimos, el usuario que estás buscando no existe o ha sido eliminado."
    />
  );
}
